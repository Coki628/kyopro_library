// グリッド斜めのループ(いつもぱっと出ないでもたつくので。。)
// rep(k, -N, N) {
//     rep(i, N) {
//         if (0 <= i+k and i+k < N) {
//             // do something below
//             // grid[i][i+k];
//         }
//     }
// }

// 右→左、下→上みたいに、状態変わる時に方向反転させたい時のdの更新
// (nxd + 2) % 4;
